{
  "name": "expense-tracker-api",
  "version": "1.0.0",
  "description": "A layered Express expense-tracker API — the subject of a from-scratch testing assignment (see docs/ASSIGNMENT.md)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "db:seed": "node src/db/seed.js",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch --passWithNoTests",
    "test:coverage": "jest --coverage --passWithNoTests"
  },
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nock": "^14.0.16",
    "supertest": "^7.2.2"
  }
}
